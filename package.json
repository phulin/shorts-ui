{
  "private": true,
  "name": "shorts-ui",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --dest build/relay/shorts-ui",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch --refresh",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "enzyme": "^3.10.0",
    "enzyme-adapter-preact-pure": "^2.0.0",
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "preact-cli": "^3.0.0",
    "sirv-cli": "1.0.3"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}
